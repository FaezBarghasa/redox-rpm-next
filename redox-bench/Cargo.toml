[package]
name = "redox-bench"
version = "0.1.0"
authors = ["Redox OS Developers"]
edition = "2024"
description = "Global Performance Auditing Suite for Redox OS"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
redox-scheme = { path = "../../redox-scheme" }

[features]
default = []
